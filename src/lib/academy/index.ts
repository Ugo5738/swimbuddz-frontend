// Barrel for the academy module.
// Re-exports everything from `./types` and `./api` so consumers can
// keep using `from "@/lib/academy"` unchanged.

export * from "./types";
export * from "./api";
